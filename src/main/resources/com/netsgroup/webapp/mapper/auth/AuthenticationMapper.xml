<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.netsgroup.webapp.mapper.auth.AuthenticationMapper">

    <select id="getCustomers" resultType="String">
        SELECT * FROM WCUST
    </select>

</mapper>
